# Pull the model

In any terminal, run:

```
ollama pull llama3.2
```

This downloads the default model (~2 GB, one time). Want a stronger model? Pull `llama3.1:8b` and set `askfirst.model` in Settings.
